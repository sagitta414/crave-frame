export function pairingEvidence(pair,prefs,memory={}){const facts=[];if(memory.favoriteMeals?.includes(pair.meal.id))facts.push('You saved this dinner as a favorite');if(memory.favoriteCuisines?.includes(pair.meal.cuisine))facts.push('Matches your saved '+pair.meal.cuisine+' cuisine preference');if(memory.shorter&&pair.show.kind==='TV episode'&&pair.show.minutes<=45)facts.push('Matches your shorter-episode preference');if(memory.preferredEffort==='Easy'&&pair.meal.effort==='Easy')facts.push('Matches your saved easy-cooking preference');facts.push(`${pair.meal.minutes} of ${prefs.cookTime} cooking minutes · ${pair.show.minutes} of ${prefs.watchTime} viewing minutes`);return facts.slice(0,3);}
