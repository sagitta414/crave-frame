# Documentation template. Export these in your shell; dev does not auto-load this file.
TMDB_ACCESS_TOKEN=
GCP_PROJECT_ID=
GCP_MODEL=gemini-2.5-flash
# Place service account JSON at .local/vertex-account.json (never commit it).
